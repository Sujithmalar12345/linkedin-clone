import { FiEdit3, FiPlus } from "react-icons/fi";
import { demoProfile } from "../data/demoProfile";
export default function Experience({ onAdd }) { return <section className="profile-card section-card"><div className="section-heading"><h2>Experience</h2><div><button className="icon-button" onClick={onAdd} aria-label="Add experience"><FiPlus /></button><button className="icon-button" aria-label="Edit experience"><FiEdit3 /></button></div></div>{demoProfile.experience.map((item) => <article className="timeline-item" key={item.role}><div className="brand-mark">{item.company[0]}</div><div><h3>{item.role}</h3><strong>{item.company}</strong><p className="muted">{item.dates} · {item.location}</p><ul>{item.description.map((line) => <li key={line}>{line}</li>)}</ul></div></article>)}</section>; }
